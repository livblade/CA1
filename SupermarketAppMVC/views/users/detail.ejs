<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <title>User Details - Ethan's Grains</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-custom">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">
        <span class="brand-icon">🏪</span> Ethan's Grains
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/inventory">Inventory</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/users">Users</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Logout</a>
          </li>
          <li class="nav-item">
            <button id="theme-toggle" class="theme-toggle nav-link">Dark Mode</button>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <div class="page-header">
      <h2>User Details</h2>
    </div>

    <% if (user) { %>
      <div style="max-width: 600px; margin: 0 auto; background: white; padding: 2rem; border-radius: 0.75rem; box-shadow: 0 2px 8px rgba(0,0,0,0.05);">
        
        <div class="mb-3">
          <strong style="color: #1e40af;">User ID:</strong>
          <p><%= user.id %></p>
        </div>

        <div class="mb-3">
          <strong style="color: #1e40af;">Username:</strong>
          <p><%= user.username %></p>
        </div>

        <div class="mb-3">
          <strong style="color: #1e40af;">Email:</strong>
          <p><%= user.email %></p>
        </div>

        <div class="mb-3">
          <strong style="color: #1e40af;">Role:</strong><br>
          <span class="<%= user.role === 'admin' ? 'badge-blue' : 'badge-green' %>">
            <%= user.role.toUpperCase() %>
          </span>
        </div>

        <div class="mb-3">
          <strong style="color: #1e40af;">Contact:</strong>
          <p><%= user.contact || 'Not provided' %></p>
        </div>

        <div class="mb-3">
          <strong style="color: #1e40af;">Address:</strong>
          <p><%= user.address || 'Not provided' %></p>
        </div>

        <% if (user.image) { %>
          <div class="mb-3">
            <strong style="color: #1e40af;">Profile Image:</strong><br>
            <img src="/images/<%= user.image %>" class="product-img" style="max-width: 200px; margin-top: 0.5rem;">
          </div>
        <% } %>

        <div class="text-center mt-4">
          <a href="/users" class="btn btn-primary">← Back to Users</a>
        </div>
      </div>
    <% } else { %>
      <p class="text-center">User not found.</p>
      <div class="text-center mt-3">
        <a href="/users" class="btn btn-primary">← Back to Users</a>
      </div>
    <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/theme.js"></script>
</body>
</html>
